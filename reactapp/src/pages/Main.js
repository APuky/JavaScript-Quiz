import React from "react";

function Main() {
  return (
    <div>
      Dashboard - main page <br /> Also the page where user will be redirected
      when they log in
    </div>
  );
}

export default Main;
